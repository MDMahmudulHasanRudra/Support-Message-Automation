export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2 bg-zinc-50 p-8 dark:bg-black">
      <h1 className="text-2xl font-semibold text-zinc-900 dark:text-zinc-50">
        Support Message Automation
      </h1>
      <p className="text-zinc-600 dark:text-zinc-400">
        Phase 1 foundation — dashboard, worker, and database are wired up.
        Real pages land in Phase 4.
      </p>
    </main>
  );
}
